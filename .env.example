# 复制本文件为 .env 并填入你的 key（或直接在系统环境变量里设置）
# 获取：https://console.anthropic.com/ → API Keys
ANTHROPIC_API_KEY=sk-ant-xxxxxxxxxxxxxxxx

# 说明：claude-agent-sdk 底层调用 Claude Code CLI。
# 若你已在本机用 `claude` 登录过，也可不配 key，走已有登录态。
