# 核心：Anthropic 官方 Agent SDK（需要 Python >= 3.10）
claude-agent-sdk>=0.1.0

# 计算：组合指标与优化（纯算法，必装）
numpy>=1.24
pandas>=2.0

# 读取 .env 里的 ANTHROPIC_API_KEY（main.py 启动时自动加载）
python-dotenv>=1.0

# 行情数据（可选：没装也能用内置离线样本演示）
akshare>=1.12

# 可选升级（高级组合优化，Windows 安装较重，按需再装）：
# PyPortfolioOpt>=1.5
